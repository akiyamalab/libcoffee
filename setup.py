from setuptools import setup

setup(
    name="libcoffee",
    version="v0",
    install_requires=["openbabel", "rdkit"],
    extras_require={
    },
    entry_points={
    },
    packages=["libcoffee"],
)
